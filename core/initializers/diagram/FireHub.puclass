@startuml

package FireHub <<Node>> #FFEBEE {
    namespace FireHub {
        class FireHub #D50000;line:9B0000 {
            + boot (Kernel_Enum $kernel):self
            - bootloaders ():self
            - registerConstants ():self
            - autoload ():self
            - kernel (Kernel $kernel):string
        }
    }
}

@enduml